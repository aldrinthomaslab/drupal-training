ctdev:
  root: /var/www/docroot
  uri: http://al.drupal-training.ctdev
  # This section is for setting command-specific options.
  command:
    site:
      install:
        options:
          sites-subdir: 'drupal_training'

nova:
  root: /var/www/docroot
  uri: http://nova.al.drupal-training.ctdev
  # This section is for setting command-specific options.
  command:
    site:
      install:
        options:
          sites-subdir: 'nova'